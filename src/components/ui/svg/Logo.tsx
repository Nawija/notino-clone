import { HeightWightTypes } from "@/src/types";

export function Logo({ h, w }: HeightWightTypes) {
  return (
    <div className="flex-c">
      <p className="text-second text-[1.6rem] font-bold uppercase tracking-wider">
        notino
      </p>
    </div>
  );
}
